# --- Core RAG ---
sentence-transformers
numpy

# --- Document loading ---
pymupdf
python-docx
beautifulsoup4
pytesseract
Pillow

# --- UI ---
streamlit
streamlit-mic-recorder     # voice input (browser speech-to-text)

# --- LLM backends ---
google-generativeai      # for LLM_MODE = "gemini" (free, used on cloud)
openai                   # for LLM_MODE = "openai"
# ollama is a separate desktop app (local only), no pip package required

# --- Utilities ---
python-dotenv
pandas
