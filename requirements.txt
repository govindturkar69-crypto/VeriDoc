# --- Core RAG ---
sentence-transformers>=2.6.0
chromadb>=0.5.0

# --- Document loading ---
pymupdf>=1.24.0          # PDFs (imported as 'fitz')
python-docx>=1.1.0       # Word files
beautifulsoup4>=4.12.0   # web pages / HTML
pytesseract>=0.3.10      # OCR (needs Tesseract installed separately)
Pillow>=10.0.0           # image handling for OCR

# --- UI ---
streamlit>=1.35.0

# --- LLM backends (install what you use) ---
openai>=1.30.0           # only needed for LLM_MODE = "openai"
# ollama is a separate desktop app, no pip package required

# --- Utilities ---
python-dotenv>=1.0.0
pandas>=2.0.0
